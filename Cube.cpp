#include <iostream>
#include <math.h>

using namespace std;

double perimeter(double);
double area(double);
double volume(double);

int main(){
	double length;

	cout << "Please key in the length of side of the cube: ";
	cin >> length;

	cout << endl;
	cout << "Perimeter: " << perimeter(length) << endl;
	cout << "Area: " << area(length) << endl;
	cout << "Volume: " << volume(length) << endl;
}

double perimeter(double length){

	return 0;
}

double area(double length){

	return 0;
}

double volume(double length){

	return 0;
}